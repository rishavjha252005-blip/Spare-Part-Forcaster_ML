# Spare Parts Forecaster Pro — dependencies
# ─────────────────────────────────────────────────────────────────────────────
# IMPORTANT: install ALL of these before running the app:
#
#   pip install -r requirements.txt
#
# On Windows you may also need to upgrade pip first:
#   python -m pip install --upgrade pip
#
# Prophet note: if prophet install fails on Windows, try:
#   pip install prophet --no-deps
#   pip install pystan cmdstanpy holidays convertdate lunarcalendar
# ─────────────────────────────────────────────────────────────────────────────

streamlit>=1.32.0
pandas>=2.0.0
numpy>=1.26.0
matplotlib>=3.8.0
plotly>=5.20.0
scikit-learn>=1.4.0
lightgbm>=4.3.0
shap>=0.44.0
statsmodels>=0.14.0
scipy>=1.12.0
prophet>=1.1.5
